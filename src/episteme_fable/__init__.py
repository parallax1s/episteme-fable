"""Episteme (Fable) — claim extraction as translation, not quotation.

Pipeline: SEGMENT -> PROPOSE (LLM) -> DISPOSE (deterministic validators)
          -> IDENTIFY -> ASSEMBLE (points, theses) -> REPORT.
"""

ENGINE_VERSION = "0.1.0"
